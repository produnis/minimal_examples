# minimal_examples
In this repo, I collect my minimal examples for QA in forums

Each example lives in its own branch


## subcaption
If you plot several plots in one chunk with `layout-ncol: 3` and `fig-subcap`, numbering of these plots is broken in (at least) PDF output.
All subcaps start with "a)" instead of counting "a)", "b)", "c)".
